import React, { FC } from "react";
import { Header } from "../layout/Header";
import "./home.css";

const Home: FC = () => {
  return (
    <div className="container">
      <Header />
    </div>
  );
};

export default Home;
